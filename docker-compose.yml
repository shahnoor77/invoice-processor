version: "3.9"

services:
  invoice-app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=fake-key
      - OLLAMA_BASE_URL=http://110.39.187.178:11434
    volumes:
      - ./knowledge:/app/knowledge
    extra_hosts:
      - "host.docker.internal:host-gateway"
    dns:
      - 8.8.8.8
      - 8.8.4.4
